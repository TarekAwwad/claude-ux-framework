# Baseline prompt (no skill)

The baseline agent receives the common body with no preamble. Full prompt =
the scenario's body (common-body.md for the dashboard scenario,
tables-common-body.md for the tables scenario), verbatim, with
placeholders substituted.
